// Authentication middleware placeholder - to be implemented later
const authenticate = (req, res, next) => {
  // Pass-through without authentication check
  next();
};

module.exports = authenticate;
